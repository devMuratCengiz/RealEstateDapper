@model List<ResultWhoWeAreDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Admin/Index.cshtml";
}

<div class="container-fluid pt-4 px-4">
    <div class="row g-4">
        <div class="col-12">
            <div class="bg-light rounded h-100 p-4">
                <h6 class="mb-4">Bim Kimiz Listesi</h6>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Başlık</th>
                                <th scope="col">Alt Başlık</th>
                                <th scope="col">Açıklama 1</th>
                                <th scope="col">Açıklama 2</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <th scope="row">@item.Id</th>
                                    <td>@item.Title</td>
                                    <td>@item.Subtitle</td>
                                    <td>@item.Description1</td>
                                    <td>@item.Description2</td>
                                    <td><a asp-action="Delete" asp-controller="WhoWeAre" asp-route-id="@item.Id" class="btn btn-outline-danger">Sil</a></td>
                                    <td><a asp-action="Update" asp-controller="WhoWeAre" asp-route-id="@item.Id" class="btn btn-outline-info">Güncelle</a></td>
                                </tr>
                            }

                        </tbody>

                    </table>
                    <a asp-action="Create" asp-controller="WhoWeAre" class="btn btn-outline-success">Oluştur</a>
                </div>
            </div>
        </div>
    </div>
</div>



